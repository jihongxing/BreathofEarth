"""
息壤（Xi-Rang）全局配置

所有策略参数集中管理，禁止散落在代码各处。
这些参数经过 Phase 2 的 6 轮调参验证，不要随意修改。
"""

# ── 资产标的 ──────────────────────────────────────────

ASSETS = ["SPY", "TLT", "GLD", "SHV"]

ASSET_NAMES = {
    "SPY": "美股大盘 S&P500",
    "TLT": "长期国债 20Y+",
    "GLD": "黄金",
    "SHV": "短期国债/现金",
}

# ── 权重配置 ──────────────────────────────────────────

# 正常模式：等权永久组合
WEIGHTS_IDLE = [0.25, 0.25, 0.25, 0.25]

# 常规保护模式：现金提升至 50%
WEIGHTS_PROTECT = [0.10, 0.20, 0.20, 0.50]

# 紧急避险模式：现金提升至 75%
WEIGHTS_EMERGENCY = [0.03, 0.07, 0.15, 0.75]

# ── 风控阈值 ──────────────────────────────────────────

# 常规保护触发线（回撤）
RISK_DD_THRESHOLD = -0.12

# 紧急避险触发线（硬止损）
HARD_STOP_DD = -0.14

# 相关性崩溃阈值（SPY-TLT 30日滚动相关性）
RISK_CORR_THRESHOLD = 0.5

# 相关性计算窗口（天）
CORR_WINDOW = 30

# ── 再平衡参数 ────────────────────────────────────────

# 偏离度阈值（绝对值）
DRIFT_THRESHOLD = 0.05

# 综合摩擦成本（滑点 + 手续费）
FEE_RATE = 0.001

# ── 冷却期 ────────────────────────────────────────────

# 常规保护冷却期（交易日）
COOLDOWN_DAYS = 20

# 紧急避险冷却期 = COOLDOWN_DAYS * 2

# ── 状态定义 ──────────────────────────────────────────

STATE_IDLE = "IDLE"
STATE_PROTECTION = "PROTECTION"
