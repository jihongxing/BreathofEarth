你说得非常对！防败家子协议确实需要 Web 界面作为基础。让我重新调整一下 90 天计划的优先级。

调整后的计划
Week 3-4: Web 界面基础（FastAPI + 前端）
为什么先做 Web 界面：

防败家子协议需要 Web 界面来展示和操作
只读面板需要可视化界面
多签确认需要 Web 表单
家族成员需要一个入口来查看和批准
Week 3-4 的具体任务：

Week 3: 后端 API（FastAPI）
创建 FastAPI 应用框架
实现核心 API 端点：
GET /api/portfolio/{id} - 获取组合状态
GET /api/snapshots - 获取历史快照
GET /api/transactions - 获取交易记录
GET /api/dashboard - 获取仪表盘数据
POST /api/withdraw - 发起出金请求
GET /api/approve/{id} - 批准出金
添加身份认证（JWT）
API 文档（Swagger）
Week 4: 前端界面（简单但实用）
只读仪表盘：
NAV 曲线图
资产配置饼图
回撤监控
调仓历史
出金申请表单
批准/拒绝界面
响应式设计（手机可访问）
Week 5-6: 防败家子协议（基于 Web）
有了 Web 界面后，再实现治理功能：

多签与时间锁
大额出金冷却期
家族成员通知
审计日志